// Comments are switched off until these two values are filled in.
// Use the project URL and the publishable key from Supabase (Project Settings, API).
// The publishable key is meant to be public. Never put a secret key here.
window.RESOLVE_COMMENTS = {
  url: '',
  anonKey: ''
};
